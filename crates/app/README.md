# dbmd-app

`dbmd-app` owns project initialization, configuration, environment expansion,
source selection, operation orchestration, exact verification, and safe
single-file/directory artifact output. The CLI maps arguments to its small
operation interfaces and presents their reports.

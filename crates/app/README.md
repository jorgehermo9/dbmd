# dbmd-app

`dbmd-app` owns project initialization, configuration, environment expansion,
source selection, local operation planning, rendering, exact verification,
operational diagnostics, agent guidance, and safe artifact output. The CLI maps
arguments to its small operation interfaces and presents their structured
reports.

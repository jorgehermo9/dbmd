# dbmd-app

`dbmd-app` owns project configuration, environment expansion, source selection,
operation orchestration, and safe artifact output. The CLI maps arguments to its
small operation interfaces and presents their reports.
